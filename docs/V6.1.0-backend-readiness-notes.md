# V6.1.0 — Notas de preparação para Backend

## Onde estão os dados hoje
- Fonte principal (frontend): `assets/data/database.json`
- Alias (compat): `data/database.json`

## O que já está “API-ready”
- `assets/js/kc-api.client.js`: cliente com fallback para banco local (`assets/data/database.json`)

## Próximos passos recomendados (sem reescrever)
1. Transformar **1 módulo** (Moradia) em dinâmico:
   - carregar posts via `KCAPI.getPosts({ module: 'moradia' })`
   - renderizar `.kc-card` via template (mantendo CSS/UX)
2. Padronizar payloads de formulários (`create-post.html`):
   - definir contrato: `module`, `category`, `tags[]`, `title`, `description`, etc.
3. Quando existir backend:
   - implementar `GET /api/v1/posts?module=...`
   - implementar `GET /api/v1/posts/:id`
