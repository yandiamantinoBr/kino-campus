# Backend Placeholder (Legado)

Este diretório é **histórico** e existe apenas como **placeholder** de uma proposta antiga de backend.

## Status
- Conteúdo legado de experimentos iniciais.
- Não é o caminho oficial de evolução do projeto no estado atual.

## Build/Deploy
- Este diretório **não participa** do build atual.
- Este diretório **não participa** do deploy atual.
- Não há instrução oficial para executar backend local a partir daqui.

## Arquitetura atual (fonte da verdade)
O app roda como:
- **Front-end estático** (HTML/CSS/JS);
- **Supabase-first** para dados/autenticação/storage.

Para detalhes operacionais e fluxo vigente, consulte o `README.md` da raiz e as migrations em `supabase/migrations/`.
