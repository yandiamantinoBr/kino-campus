# Registro de descontinuação do `sql/` na raiz

O antigo `sql/README.md` foi preservado aqui apenas como evidência histórica.

Na V8.2.0.1, o diretório `sql/` da raiz foi removido para eliminar ambiguidade operacional.

Fluxo oficial de banco (único válido):

- `supabase/schema-*.sql`
- `supabase/migrations/*.sql`
