# SQL movido para legado

Os scripts SQL antigos foram movidos para `docs/legacy/sql/`.

- Use este diretório apenas para navegação.
- Para operação/deploy, siga o fluxo oficial em `supabase/` (schema e migrations).
